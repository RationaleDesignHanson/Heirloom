{
  "name": "heirloom-seed",
  "version": "1.0.0",
  "description": "Seed demo creators and public recipes for Discovery feature",
  "type": "module",
  "scripts": {
    "seed": "npx ts-node --esm src/seed.ts",
    "seed:no-images": "npx ts-node --esm src/seed.ts --skip-images",
    "verify": "npx ts-node --esm src/verify.ts",
    "cleanup": "npx ts-node --esm src/cleanup.ts"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "dotenv": "^16.4.0",
    "replicate": "^0.29.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.10.0"
  }
}
